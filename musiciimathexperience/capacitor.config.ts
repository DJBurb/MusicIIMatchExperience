import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.musiciimathexperience',
  appName: 'musiciimathexperience',
  webDir: 'www'
};

export default config;
